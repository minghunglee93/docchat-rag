version: '3.8'

services:
  api:
    build: .
    image: docchat-rag:latest
    container_name: docchat-rag
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./vector_db:/app/vector_db
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    restart: unless-stopped